import { StyleSheet } from "react-native";

export const gStyle = StyleSheet.create({
  main: {
    flex: 1,
    padding: 20,
    paddingTop: 32,
    alignItems: "center",
    backgroundColor:'white',
  },

  title: {
    fontSize: 20,
    color: "#333",
    textAlign: "center",
  },
});
